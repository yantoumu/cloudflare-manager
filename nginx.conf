# Nginx 反向代理配置
# 将此配置添加到你的 Nginx 配置中

server {
    listen 80;
    server_name your-domain.com;  # 修改为你的域名或服务器IP

    # 如果你已经配置了 HTTPS，改为:
    # listen 443 ssl http2;
    # ssl_certificate /path/to/your/cert.pem;
    # ssl_certificate_key /path/to/your/key.pem;

    # 客户端最大上传大小（用于上传 Worker 脚本）
    client_max_body_size 10M;

    # 主应用代理（前端 + API）
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;

        # WebSocket 支持
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';

        # 标准代理头
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 禁用缓存（动态应用）
        proxy_cache_bypass $http_upgrade;
        proxy_no_cache 1;

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # WebSocket 专用路径（可选，如果上面的配置不生效）
    location /socket.io/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 健康检查（可选）
    location /health {
        proxy_pass http://127.0.0.1:3000/health;
        access_log off;
    }

    # 访问日志
    access_log /var/log/nginx/cloudflare-manager-access.log;
    error_log /var/log/nginx/cloudflare-manager-error.log;
}

# 如果你的 Nginx 已经有 HTTPS，HTTP 自动跳转 HTTPS
# server {
#     listen 80;
#     server_name your-domain.com;
#     return 301 https://$server_name$request_uri;
# }
